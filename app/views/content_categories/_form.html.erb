<%= form_for(@content_category) do |f| %>
  <% if @content_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_category.errors.count, "error") %> prohibited this content_category from being saved:</h2>

      <ul>
      <% @content_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
